# 新项目交接

最后修改日期：2026-09-09

## 从这里继续

这是已复制好的独立新项目，lxy 下次会提供新运维与 TikTok 配置。先读根目录 AGENTS.md，再读 config/配置填写说明.md。当前工作为项目隔离、启动优化，以及 2026-09-09 新要求的在线时长含广告并改为15秒记录；玩法与广告节点保持原样。

## 已完成

- 两个新目录独立初始化 Git 并设置指定 origin；没有复制旧 .git、提交、推送或访问远端。
- 复制当前工作区的游戏资产、代码和服务端逻辑，保留已有 Prefab/Scene/.meta；未复制旧构建缓存、运行环境文件和私钥。docs/fork-manifest.json 记录复制时的源 commit、状态和文件哈希。
- 新客户端公开配置入口及 development/staging/production 预留；生成文件一致性、旧目标识别、秘密字段检测和发布锁。
- 本地存储、TikTok 全局配置和构建缓存改为 BT 独立命名；无旧 API 默认地址，正式包禁止模拟平台兜底。
- 启动继续并行登录/换票/资源下载；本次额外将首页平台功能开关请求提前并复用同一结果，首页仍等待该结果。未删除任何必需初始化，未新增广告或改变按钮行为。
- 新后端进程/部署模板/本地 Compose 命名；API/migrate 连接前校验新 BT 环境和独立库用户；云环境关闭游客和测试账号入口。未改 migration、经济规则或统计查询。在线时长新增纯逻辑累计器，广告区间与前台时间互斥，15秒分段；后台沿用当前 LinkGame 优化版展示，只增补口径文案。
- 老项目文档归档 docs/legacy，不再作为当前发布指南。

## 本次尚未做（不能误认为完成）

没有新云配置，没有正式 TikTok 构建/上传、数据库初始化、真实平台登录/广告、真机启动成功率或统计对账验证。两端发布链路已接上配置预检，当前 releaseEnabled 均为 false。服务端配置生成同时产生部署模板和远端环境比较；填配置后无需再删锁或改发布代码。配置本身及新环境真机联调仍未完成。

现有主题等窗口的按需加载策略是复制时已有的，不代表本次已完成进入后流畅度验收。新环境真机验收要观察首次点击等待和帧耗时，出现卡顿应在不改玩法前提下调整资源调度，再复测。

## 下次配置接入顺序

1. 核对新的 API、数据库、CDN、SSH、TikTok 应用和广告位，填写配置说明指定的位置，秘密只保存在忽略文件/远端环境中。
2. 独立新数据库及最小权限用户就绪后，在新测试环境运行原有向前 migrations，再验登录、云存档、奖励幂等和健康检查。
3. 客户端切到 staging 并生成配置，核对 API/CDN/平台同属新游戏；完成发布预检后制作测试包。
4. 真机覆盖冷缓存/热缓存/弱网/断网恢复、新号首关引导、老号首页、全部常用入口、失败重开/下一关、广告完整生命周期和前后台恢复。
5. 对比 TikTok 启动统计与内部匿名启动、登录成功、enter_game 事件；事件时间/账号/会话/去重符合已有定义。不要用提前 ready 或模拟登录改善数字。
6. 记录启动耗时分位数、失败阶段、进入后按钮首次响应时间和帧卡顿，再决定是否需要更细的加载调整。未经真机测试不承诺启动成功率提升百分比。

## 新对话可直接发送

“这是 LinkGameBt 新海外 TikTok 项目。先读 AGENTS.md 和 docs/新项目交接.md。游戏逻辑和现有广告保持原样；这里是新项目配置，请按预留位置接入，并完成启动及进入后可操作性、统计准确性验收。旧 LinkGame 环境不可使用。”

## 2026-09-09 接续任务完成项

- 两端 AGENTS、配置生成、发布前检查与配对检查可直接交接。
- 后台来源仍是 LinkGameServer commit 74ecb7f，保留新增 cohort、趋势、排行、留存等优化展示和原 SQL。
- online_time 每15秒记录；广告 show/close 区间计入、普通后台不计入，长广告可补多个15秒事件。队列/重试/服务端幂等契约不变。
- scripts/test-analytics-online-time.mjs 已覆盖真实累计器与真实 GameAnalyticsService 队列接线；自动测试结果见 docs/本次验证记录.md。
- 下一步主要是填入新运维/平台配置、初始化新测试库和真机联调，不需要再复制项目。两端 Git 已设置 origin，尚无提交/推送。
