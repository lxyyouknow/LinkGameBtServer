# 本次验证记录

最后修改日期：2026-09-09

## 已通过的本地检查

- 客户端15个回归脚本全部通过：关卡168/168、寻路/棋盘/移动、云每日挑战/赛季、排行榜、主题尺寸、启动、TikTok广告回调模拟、统计漏斗、在线时长、新BT配置。
- 使用 Creator 3.8.8 正式引擎声明完成全客户端 TypeScript 严格检查；未复制旧 temp/library。
- 在线时长新增行为测试与真实 GameAnalyticsService 队列测试：15秒分段、广告挂起补算、早关、失败、普通后台排除、重复事件、切会话迟到回调、区间并集、异常上限。
- 服务端 gofmt、env MYSQL_TEST_DSN= go test ./...、go vet ./... 通过；新BT环境预检、15秒/尾段协议以及云环境关闭游客登录均覆盖。
- Linux amd64 构建成功，产物包含 API/migrate/check-config 和原 migrations、远端字段比对脚本；构建包位于服务端 dist。
- 两端空配置实际运行发布入口均在联网前退出，返回预期的配置错误。秘密文件忽略规则与两个新 origin 已核对。
- 复制核验：7,479个已有资源/Prefab/Scene/meta字节一致；12个model文件除存储前缀/对应import外一致；服务端92个migration/store实现文件一致。源工程文件仍与复制时哈希一致。
- 项目JSON全量解析通过（不包含标准JSONC的tsconfig.json与忽略的输出目录），详见 fork-verification.json。

## 基线调整说明

旧项目 test-leaderboard.mjs 的3张图硬编码哈希中存在与当前工作区不一致的基线；在旧项目也复现同样失败。新测试改为核对复制时的 fork-manifest 哈希，图像和Prefab本身未改。Node广告测试补充 cc/env 测试桩来运行原有真实适配器测试。

## 尚不能声称完成

新环境/TikTok参数尚未提供，因此未做 Creator 正式平台包、平台上传、CDN可达性、MySQL集成测试/迁移、真实TikTok登录/广告回调、手机启动成功率与帧表现、端到端统计对账。Go数据库集成测试通过环境条件跳过，不代表实库验证通过。

Windows PowerShell 发布脚本只做了代码审查和门禁接线，本机没有 PowerShell，未执行Windows构建。Mac/Linux构建脚本语法与本机构建已检查。

没有 commit/push/SSH/CDN上传/部署。新的 Git 目前是独立 main 空历史工作区，已设置用户指定 origin。
