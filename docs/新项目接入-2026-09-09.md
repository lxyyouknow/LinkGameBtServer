# 新 BT 第一版接入

最后修改日期：2026-09-09

新服务已部署到 https://linkgamebt.bffbond.com，后台 /admin/latest，独立 systemd 用户服务 linkgame-bt.service，端口23001，schema41。2026-09-09 最新服务版本20260909081026。

当前 staging/tiktok-native，Client Key mgtbarscemwda9wa 对应 App ID7683336707487434773。Secret 只在忽略的本机环境和远端 shared/app.env 中。allowWebPreview=true 只为保留 lxy 要求的 Web 测试入口，身份/provider 与 TikTok 隔离，production 禁止。后台默认 TikTok，上线数据不能与 Web/测试混看。

自动检查：go test ./...、go vet ./...、配置生成一致性、两端配对通过。新数据库 API 通过账号隔离、存档回读、奖励重复请求防重、事件三次重放防重；在线时长15+15+7=37秒对账通过。真机登录/广告与试玩由 lxy 验收，自动通过不代表已完成真机验收。

本机后台账号文件 .env.admin-access（忽略、600权限），禁止复制到客户端或文档。原服务版本20260909030941保留，环境变更前备份为 shared/app.env.before-tiktok；回退时须同时恢复匹配的环境文件与 current 链接，再重启检查健康。
